# Notes

Raw observations from any phase. Prefix with phase name. Distilled into `docs/lessons-learned.md` during QA.

<!-- Append below this line -->

[spec_review] XS tier work has stale hard-coded "fast-tier (S)" references outside the spec's named policy/docs surfaces, including runtime prompt templates, prompt goldens, canon-owned operator skills, CLI help, and README. A size-policy spec needs an explicit grep AC for old tier labels across shipped guidance surfaces.

[spec_review] `sync-canon-templates` only mirrors paths in `CANON_OWNED`; among protected docs, current `CANON_OWNED` includes `docs/pipeline-orchestrator.md` but not `docs/product-context.md`, `docs/decisions.md`, or `docs/architecture.md`. Specs that call those `templates/docs/*` files auto-synced need either a managed-set change or hand-edited template rows.

[spec_review] Fast-tier policy changes affect orchestration tests outside `tests/pipeline-policy.test.ts`; current reroute/full-send tests seed `task_size: 'S'` specifically to exercise fast-tier paths, so an XS-tier spec needs an explicit non-policy test fixture sweep.

[spec round 5] Round-over-round shape check (4 spec_review rounds, all changes_requested): the SECOND DIMENSION (stale-label sweep) is SCOPE-EXPANSION, not edge-fine-tune — each round surfaced a new label *family*, not a missed instance of the same family. A structural sweep over the whole repo found FOUR distinct families: (A) "fast tier = S" identity labels [the only family rounds 1-3 chased]; (B) "M/L/XL/delicate" full-tier/spec_review membership enumerations that omit S [round 4 issue 2]; (C) size-set enumerations omitting XS (`<S/M/L/XL>`, `S | M | L | XL`, env-var `S/M`/`S/M/L` tiers); (D) a genuine TERMINOLOGY COLLISION the spec never addressed — README + canon-inline-review SKILL already use "XS" to mean "inline work too small for a canon task," which directly contradicts the new formal XS = smallest *pipeline* task. Per the spec-authoring rule, redesigned AC-18 from a single hand-narrowed grep into an explicit post-change INVARIANT + a per-family structural gate (lookbehind for B since `S/M/L/XL` contains `M/L/XL`; positive targeted ACs for C since the fixed enum contains the old one), with AC-19 as a complete worklist subordinate to the gate.

[spec round 6] spec_review round 5 raised ONE blocking issue (a different dimension than rounds 1-4's guidance-sweep scope-expansion; round-5 Shape Check said "No concerns" on the sweep): non-policy orchestration tests seed `task_size: 'S'` to exercise fast-tier paths and will break/invert when S graduates. Swept the full `task_size: 'S'`/`taskSize: 'S'` set in tests/ and classified each: behavior-DEPENDENT (must move S→XS) = `run-task-reroute-preflight.test.ts` (fast-tier reroute test L684 + helper type L38-40 excludes XS) and `run-task-safety.test.ts` (two fast-tier spec-gate tests L2391/L2445). Behavior-AGNOSTIC (S is an arbitrary valid size; assertions don't depend on tier) = canon-snapshot:26, validation:125/1610, counter-schema:21, prompts:111, ship:214, task-cli:76 — leave unchanged. Confirmed the prompts golden's `full / S` comes from an explicit `tier:'full'` override in makeTask (prompts.test.ts:165), NOT policy, so S→full doesn't touch it — only AC-21's rule-of-thumb flip changes the golden. Edge-fine-tune (one bounded, fully-enumerated family), not scope-expansion → fix + mark done. Added AC-12b + two test rows to Affected Files.

[spec round 5] Round-4 issue 1 confirmed against code: `sync-canon-templates.mjs` syncs only `WHOLESALE_SYNC = [...CANON_OWNED]`. Of the docs this task edits, only `docs/pipeline-orchestrator.md` is in CANON_OWNED, so only `templates/docs/pipeline-orchestrator.md` auto-syncs. The other three `templates/docs/{product-context,decisions,architecture}.md` exist as unmanaged scaffold snapshots that sync never touches and that won't appear in the task diff. Removed them from the generated-mirror table; fixed AC-22 to claim sync only for actually-edited CANON_OWNED files (adds `.claude/skills/canon-inline-review/SKILL.md` mirror via family D).

[spec round 7] spec_review round 6 raised ONE blocking issue — a single missed *instance* of Family A (edge-fine-tune, NOT a new family per the round-over-round rule): the `**Fast tier** (all tasks S, non-delicate)` header at `docs/pipeline-orchestrator.md:151`, which the Family A grep's literal `(S…)` shapes didn't match and which the AC-14/AC-19 worklist omitted. Fix = redesigned Family A's gate from brittle literal shapes into a word-bounded INVARIANT `\bS[\s,)\x60]*non-delicate` (PCRE/`-nP`) that catches every separator variant (comma/backtick/paren/space) and cannot match post-change `XS` (the `S` is always preceded by `X`). Re-sweeping with the new pattern surfaced TWO more Family-A surfaces the old gate+worklist also missed — `docs/pipeline-orchestrator.md:431` (`S, non-delicate tasks re-enter`) and `docs/product-context.md:90` (`` `S` non-delicate ``) — plus `docs/decisions.md:87`; all added to AC-19 A / AC-14 / AC-16a. Excluded `tests/pipeline-policy.test.ts` from Family A ONLY: its `{ name: 'S non-delicate' }` / `only S non-delicate` labels describe a still-existing S task (S keeps existing, only its tier flips), not stale guidance; governed positively by AC-12. Golden kept in-gate (AC-21 regen flips it). VERIFIED all three zero-gates (A/B/D) exactly as written: every matched file is in the AC-19 worklist or is the golden → gates return zero post-impl; the "Family B outside worklist" probe was empty (analysis/telemetry docs carry no gated patterns). Same completeness pass closed Family C's missed shared-band siblings — XS clones S so it joins every S-band: env-var rows `docs/pipeline-orchestrator.md:243,245,338`, policy.ts band comments `43,122,193` (not the historical `120` "old caps"), `canon-status/SKILL.md:63`, test `161,226`; added canon-status to AC-22 sync set + Section D mirror. Excluded historical/telemetry band refs (policy.ts:120, task-quality-log, harness-audit-2026-06, canon-opus48-gpt55-report) and noted them in Non-Goals + AC-19 C. This is the round-5 "sweep comprehensively, don't drip-feed" philosophy applied to Family A's gate + Family C's worklist. Marked spec done → spec_review round 7.

[implement] The planned `canon-spec` updates fixed the explicit light/grill split, but the nearby "Topics to work through for M+" shorthand would have understated S after S became full tier. Changed it to "full-tier tasks" and documented the extra consistency edit in handoff deviations.
