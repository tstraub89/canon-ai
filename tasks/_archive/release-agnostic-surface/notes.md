# Notes

Raw observations from any phase. Prefix with phase name. Distilled into `docs/lessons-learned.md` during QA.

<!-- Append below this line -->
[implement-revision] Backtick refs to a missing repo doc in handoff/review trip docs-refs-check even when they're explanatory prose. Use bare prose for missing `docs/decisions.md` references.
[implement-reroute] AC-12 wants the versioning-policy doc read in Phase 3 when present, but the absent-case nudge path still needs to stay separate; the instruction has to say "when present" before the generic heuristics so the optional policy doesn't become a hard dependency.
[implement-reroute] AC-13's release-branch init enumeration needs canon-ai's npm/.canon/version steps framed as an example, not a universal requirement, or non-npm adopters read the skill as prescribing their bootstrap flow.
[implement-reroute] AC-14 needs the finalize path to branch on version-carrying vs. version-less unreleased headings; otherwise the Keep-a-Changelog default loses the proposed version when it finalizes.
[implement-reroute] AC-15's sweep exposed more npm-specific wording than the previous rounds covered: version source, fresh-release / in-progress version files, diff preview, and commit-message templates all needed to be rewritten as project-level examples instead of canon-ai-only mechanics.




