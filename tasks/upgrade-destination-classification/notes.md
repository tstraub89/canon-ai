# Notes

Raw observations from any phase. Prefix with phase name. Distilled into `docs/lessons-learned.md` during QA.

<!-- Append below this line -->
[spec] Drafted from #187 + BACKLOG triage. Judgment calls made solo (operator pre-scoped): (1) git-error handling = degraded classification (existing destinations refuse, absent still scaffold) rather than hard-fatal — preserves greenfield non-repo path and existing fixtures at cli.test.ts:901/931/2099; (2) gitignored-existing folded into untracked-existing (same git-unrestorable class; plain porcelain can't see it); (3) size M + delicate:true (refusal semantics on a data-safety boundary, adopter-wide). Explorer found cli.test.ts:2211 asserts the buggy behavior by name — AC-1 inverts it.
[spec_review] /canon-spec-review (3 sub-agents): 0 BLOCKING. Applied: AC-5b (--force must NOT override malformed .gitignore — regression guard, flagged by 2 agents independently as widened-force gap); AC-8b (mixed pending set — all-or-nothing withholds writable target too); fixed inverted ls-files citation (index.ts:1322 drops --exclude-standard deliberately, which actually supports AC-4). All 12 factual claims verified green by ground-truth agent. Holding at human_spec_gate — pipeline NOT launched.
[spec_review] Current runUpgrade write-surface audit: besides the five sources named in the spec, docs-refs cutover scaffolds scripts/docs-refs-config.mjs through its own pending.push() when the config is missing. Because the spec routes classification at the shared pending gate this is a nit, not a blocker, but implementation should include it in the mental model.
[spec] Amended post-spec_review to fold in Codex's 3 nits (approved_with_nits, round 1): (1) AC-13 + Decision/Interaction-Deps text — docs-refs-config.mjs is a 6th write source, not counted in original 'five sources' claim; (2) AC-3b + Decision classification-order note — locally-deleted tracked file must stay tracked-dirty, not collapse into new 'absent' class (existing behavior at cli.test.ts, guards against existsSync-first implementation); (3) AC-8 reworded — parity is normalized classification equality across wouldUpgrade/dirtyRefused/upgraded field names, not literal same-field identity. No phase state changed — still holding at human_spec_gate, spec_review already done/approved_with_nits. Not re-invoking pipeline without operator go-ahead.
[plan] Spec gap: AC-13's literal wording ("A test with an untracked, non-identical scripts/docs-refs-config.mjs already present... refuses it as untracked-existing, the same as any other managed target") is in tension with the pre-existing, explicitly-tested "config already present → NOT re-scaffolded, fully adopter-owned, untouched regardless of content" contract (tests/cli.test.ts "pre-split checker with config already present is overwritten WITH a warning" and "new docs-refs checker with config present upgrades normally and does not scaffold"). A fully literal "same as any other managed target" reading would make docs-refs-config.mjs symmetric with the CANON_OWNED loop — i.e. overwritten whenever non-identical, including tracked-clean — which would silently destroy committed adopter customizations (the exact class of bug this task fixes, reintroduced for this one file). Neither the Decision table nor Known Risks resolves this explicitly; spec_review round 1 flagged docs-refs-config.mjs only as a missing "6th write source," not this specific tension.
Plan-time resolution (see plan.md Step 4): keep the "adopter fully owns tracked-clean content forever" invariant untouched, but replace the current pure-existsSync "missing" gate with a classifier-informed one: absent → scaffold (unchanged); tracked-but-locally-deleted → refuse (new, closes a parallel AC-3b-shaped gap for this source specifically); exists + non-identical + untracked-existing/unverifiable/tracked-dirty → refuse (satisfies AC-13's literal test scenario); exists + non-identical + tracked-clean → leave untouched (preserves the existing adopter-owned contract). If QA or the human disagrees with this reading, the fix is isolated to the `docsRefsConfigClass` branch in Step 4 — nothing else in the implementation depends on this specific interpretation.
