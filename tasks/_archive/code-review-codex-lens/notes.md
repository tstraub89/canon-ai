# Notes

Raw observations from any phase. Prefix with phase name. Distilled into `docs/lessons-learned.md` during QA.

<!-- Append below this line -->
[spec_review] `templates/docs/decisions.md` and `templates/docs/product-context.md` exist in the tree but are not auto-synced by `npm run sync-templates`; `sync-canon-templates.mjs` derives wholesale sync from `CANON_OWNED`, which currently includes `docs/pipeline-orchestrator.md` but not those docs.
[spec_review] Deliberately leaving `code_review` not-done with a populated `review.md` is not an immediate halt: `checkAndRoute()` runs evidence recovery and then a one-shot retry, so a no-verdict review artifact can resume the Claude review session before the human-stop path.
[spec_review] A raw branch diff is not automatically spec-blind in canon worktrees: task artifacts live under `tasks/<id>/`, `handoff.md` is explicitly written during implement, and `getScopedDiff()`/`codex review --base` have no task-artifact exclusion by default.
[spec] Resolved the spec-blindness Shape Check blocker by precision, not new machinery. Verified empirically: at `code_review` time `handoff.md` (and `spec.md`/`plan.md`/`notes.md`/`status.json`) is UNCOMMITTED — `autoCommitCode()` stages only handoff-table source files (`main.ts:512`,`:605`–`614`); task artifacts commit at QA-end (`patterns.md` "Operator git surgery…"). `getScopedDiff()` is committed-only (`git diff <base>...HEAD`, `git.ts:195`), and the existing cold-Claude lens reviews exactly that surface (`code-review-foreman.md:54`). So the reviewer's premise that `handoff.md` rides in the cold review diff does NOT hold for the committed `<base>...HEAD` range — the reviewer conflated `TASK_ARTIFACT_FILES` ("tracked" name set, `worktree.ts:26`) with "committed in the diff." Fix: AC-3 requires the cold-Codex lens to review the SAME committed surface as cold-Claude (implementer verifies `codex review --base` scope; pin to committed range if it folds in the working tree) + AC-14(e) positive guard test. Did NOT add an artifact-filtering construction — it would make cold-Codex more spec-blind than cold-Claude and break the same-surface symmetry.
[spec] Iteration-shape read at this round: spec_review counters are high (iterations_total 10, changes_requested_total 10, auto_block_count 2), but the shape is CONVERGING, not scope-expanding — R1 (orchestrator-run hard-fail) and R2 (run-log-only telemetry) produced durable resolutions that have not reopened, and the large Non-Goals list reflects steady de-scoping toward minimal-viable. The current blocker was a single-AC framing-precision issue (one factual error about the diff surface), so a targeted fix is correct over a carve-out. If round 4 surfaces a NEW sub-problem rather than approving, re-evaluate for carve-out per `docs/lessons-learned.md` (≥3-round gate sub-problem → carve out).
[implement] Direct targeted test runs must include the repo's Markdown import hook; `node --test --import tsx tests/run-task-prompts.test.ts` fails on `.md` template imports, while `node --test --import ./tests/md-loader-register.mjs --import tsx ...` matches the npm test environment.
[implement] After `npm run build`, `git diff --exit-code -- dist/` is nonzero before auto-commit because `dist/scripts/run-task.js` is expected generated output from source changes and commit staging is orchestrator-owned; list the dist file in handoff instead of trying to commit manually.
