# Code Review: reroute-latest-amendment-section

> Reviewer: Claude | Spec: `tasks/reroute-latest-amendment-section/spec.md`
>
> **Per-round sections.** This file is cumulative across review rounds. The Stage 1 / Stage 2 structure below covers Round 1 (initial review). On re-review, append a new `## Round N` section near the bottom rather than rewriting earlier rounds — Codex reads only the latest round's section to know what to address.

Review runs in two stages on the first round. **Stage 1 is a gate.** If it fails, skip Stage 2 entirely and send back — do not write code-quality findings against code that's about to change.

## Stage 1 — Spec Compliance (gate)

### Validation Gate

- [x] Validation Outcomes table has no `Fail` results — all Pass; `not_configured` entries have spec citations.
- [x] All checks required by the spec's "Validation Required" section were run — lint, test, build, type-check all Pass; sync-templates:check and E2E are spec-marked N/A.
- [x] No required checks were skipped without justification.

### Acceptance Criteria Check

| AC | Status | Notes |
|---|---|---|
| AC-1: Last same-round section wins | Met | First pass sets `start = i` unconditionally on every match — old `if (start === -1)` guard fully deleted. Duplicate-round-2 test asserts `Approved` returned, `Changes requested` absent. |
| AC-2: Single-section and absent cases covered | Met | Tests added: single round-2 match (exact slice asserted), null no-match, round-1 bare-label duplicates selecting the latest bare heading. |
| AC-3: Fence/comment-awareness preserved, tracked continuously from file start | Met | Both passes start from `i = 0` and carry `inFence`/`inComment` across all preceding lines including earlier same-round sections. Fenced-heading test covers AC-3a; fence opened in first real section and closed before last real heading covers AC-3b. |
| AC-4: `checkRerouteEvidence` reads fresh verdict end-to-end | Met | `checkRerouteEvidence('spec_review', ...)` test with stale `Changes requested` followed by fresh `Approved` asserts `{ reroute: true, ok: true, verdict: 'approved' }`. |

### Dropped Sections Check

- [x] Non-goals respected — no `reroute_count` change, no routing change, no heading convention change.
- [x] Known Risks addressed — end-boundary regression guarded by fence-carry test; round-1 duplicate case has its own test.
- [x] Human Test Plan satisfiable — the recovery flow is exercised by AC-4's end-to-end test.

### Stage 1 Verdict

- [x] **Pass** — proceed to Stage 2

## Stage 2 — Code Quality (only if Stage 1 passed)

### Summary

Clean two-pass implementation: first pass scans the full file updating `start` unconditionally on every round-matching heading (last match wins); second pass rescans from `i=0`, carrying fence/comment state continuously, skipping `i <= start`, and returning the slice when the next real `#`/`##` heading is found. The spec's hard constraint — fence state tracked from file start, not restarted at the chosen heading — is met in both passes. Tests are comprehensive and map 1:1 to spec ACs.

### Findings

**`spec gap` — `dist/cli/index.js` undeclared in spec's Affected Files; `--pr` base-drift gate will reject it**

The spec's Affected Files table lists only `dist/scripts/run-task.js`. `npm run build` regenerates `dist/cli/index.js` from the same source — Codex correctly rebuilt both and documented the deviation. The `--pr` base-drift gate allow-list is built from `task-dir + telemetry + spec's Affected Files`; `dist/cli/index.js` is absent from all three and will cause a gate rejection at ship time (CLAUDE.md §"Build-generated artifacts go in Affected Files").

Fix before running `--pr`: add a row to `spec.md` §Affected Files —

```
| `dist/cli/index.js` | Regenerated by `npm run build` — `validation.ts` is also bundled into the CLI entrypoint. |
```

No Codex re-run required.

## Final Verdict

- [x] **Approved with nits** — implementation is correct and complete; amend `spec.md` Affected Files to add `dist/cli/index.js` before running `--pr`

---

<!--
On re-review, append below this line:

## Round N — verifying iteration N-1's response to round N-1

### Stage 1 — Acceptance Criteria Re-Check

Re-fill this table with every AC from spec.md against the latest code. Earlier AC tables were snapshots of earlier iterations, not reusable proof. ACs whose relevant code paths did not change may be marked `Met (unchanged from round N-1)` with a one-line evidence pointer.

| AC | Status | Notes |
|---|---|---|
| AC-1: ... | Met / Partial / Not Met | ... |
| AC-2: ... | Met / Partial / Not Met | ... |

### Verifying Round N-1 findings

- _correctness bug:_ "<one-line summary>" → addressed (file:line; AC-N now Met in table above) ✓ / still open / no longer relevant
- _risk/guardrail:_ ... → ...

### New findings (only NEW issues introduced by Iteration N's changes)

(none / list)

### Verdict for this round

- [ ] Approved
- [ ] Approved with nits
- [ ] Changes requested
- [ ] Needs re-review

> Round 3+: findings must be `correctness bug` or `spec gap` only — no `optional cleanup/nit` and no wording-only changes. We are tightening, not exploring.
-->
