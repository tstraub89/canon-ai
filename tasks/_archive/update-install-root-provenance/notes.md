# Notes

Raw observations from any phase. Prefix with phase name. Distilled into `docs/lessons-learned.md` during QA.

<!-- Append below this line -->

[implement] Importing the shared CANON_UPSTREAM_REPO constant also imports env.ts, whose module initialization performs a local git probe; the red-first fake-git fixture therefore sets GIT_TERMINAL_PROMPT=0 in the child environment and asserts the resolver path remains non-interactive.

[implement] docs-refs-check treats backtick paths as repository references. The generated provenance path is documented as provenance.json under .canon so the docs explain the file without claiming a not-yet-created repository path.

[implement-revision] Nested node_modules paths need a local-shaped detection branch so pnpm virtual-store installs reach the ordered layout gate rather than falling through to the global npm path.

[implement-revision] When AC-3 accepts canon-ai from multiple dependency blocks, matching npm save flags preserve the manifest's existing dependency classification instead of silently moving it to devDependencies.

[implement-revision] Generated dist must be rebuilt after every source-only correction; the prior strict-tag source change had landed after the generated bundle was produced.

[spec_review] Fresh Round-5 review: the revised AC-2/AC-6 red-first tests require new `updateCmd(args, deps?)` seams, but the current `updateCmd(_args)` ignores a second argument and directly calls `detectInstallType()`/`spawnSync()` (`src/cli/commands/update.ts:26-47`), so a baseline red run cannot exercise the fixture or fake npm/resolver. AC-10's schema checks cover only channel, SHA, and stable version even though the declared persisted shape also requires `source` and `updated_at` (spec lines 103-105), and the absent branch has no defined outcome when both `.canon/provenance.json` and `.canon/version` are missing. The current backlog still describes `--ref <sha>` (docs/BACKLOG.md:364-367), but the revised resolver only queries named remote refs, so the spec does not implement that stated flag contract.

[spec_review] `update.ts` derives `packageDir` at module load from `import.meta.url`, while `updateCmd()` has no package-dir/install-detector injection; a direct source-import test cannot place the running package in the AC-2 fixture without an additional seam or subprocess strategy.

[spec_review] The revised spec makes `CANON_UPSTREAM_REPO` choose the repository queried by the resolver, but AC-6 still requires the npm target to use the hard-coded `github:tstraub89/canon-ai` slug; a fork override can therefore resolve a SHA that is not reachable from the install target.

[spec] Round-3 revision (Claude) — resolved all 4 blocking spec_review issues, all edge-fine-tune/contract-consistency (not scope-expansion): (1) single effective slug drives resolver remote + npm source + provenance `source` (Decision item 5, AC-6 fourth case) — closes the fork-override incoherence in the note above; (2) bare `X.Y.Z` version (strip leading `v`) for persist + doctor compare (Decision item 7, AC-5/AC-9/AC-10d); (3) defined `checkCanonProvenance(cwd, deps?)` exported pure check with poison `runCommand` seam, test targets the pure check (AC-10); (4) explicit dev-ref resolver contract — `git ls-remote <url> <refspec>`, peeled-commit, fail-closed on zero/ambiguous (Decision item 6, AC-8). The packageDir-seam note above was already covered by AC-2's `deps.packageDir` override.

[spec_review] Current revision leaves doctor branch precedence and the provenance lookup root unspecified: a stable file with a mismatched version satisfies both the stable-pass and stale-mismatch branches, while update writes local provenance at `installRoot` but `doctorCmd()` currently passes `process.cwd()` to its repo checks.

[spec] Round-5 revision (Claude, conversational, operator-directed) — SCOPE SPLIT, not another patch. Round-shape check: doctor/AC-10 produced new blocking findings in BOTH rounds 4 and 5 (branch overlap + malformed + lookup-root, then missing required-field validation + both-files-absent undefined) → that's the spec's own pre-declared trim trigger; and the red-first seam objection was a round-1 finding re-surfacing verbatim in round 5 (asserted-fixed in round 3, never mechanism-fixed). Operator decision: (1) TRIM doctor provenance-reading (old AC-10) to a follow-up — new BACKLOG entry "canon doctor reads install provenance" under Distribution & Portability, which also captures the evaluate-first idea of deriving identity from the adopter's own pinned package.json dependency spec instead of the bespoke file; doctor.ts now untouched (Non-Goals structural backstop: no doctor.ts hunk in the diff). Provenance WRITE stays (AC-9) so the follow-up has a shipped format to read. (2) Red-first mechanism replaced: seam-free subprocess fixture mirroring the reporter's #188 repro — copy committed dist/ + package.json into install/node_modules/canon-ai/, invoke that dist entrypoint from adopter/ with PATH-shimmed fake npm (recorder) + fake git (canned ls-remote transcript); pre-fix dist goes red with zero seams, resolving the round-5 "seams don't exist pre-fix" paradox; updateCmd(args, deps?) seam demoted to post-fix unit plumbing. (3) --ref <sha> gap closed by simplification: 40-hex input IS already immutable → skip ls-remote, pin directly, fail-late at npm fetch (new Non-Goal: no reachability pre-check). ACs renumbered: old AC-11/12 → AC-10/11.

[spec_review] Fresh Round-6 review: the revised spec simultaneously says provenance is write-only/no reader ships and requires the updater announcement to read it for current version/SHA (Decision item 4 vs item 7/Data Model/AC-5), without defining malformed-read behavior. Refusal and failure ACs require post-fix unit tests, but `updateCmd` currently exits through `process.exit` (`src/cli/commands/update.ts:47-50`) and the specified deps seam omits an exit override, unlike existing `stopCmd`/`watchCmd` seams. The docs AC asks the canon doctor row to reflect recorded provenance while the same spec forbids doctor changes and moves its reader to a follow-up; wording must not imply current doctor consumption.

[spec] Round-6 revision (Claude, conversational) — all 3 blockers were internal-consistency slips introduced BY the round-5 split rewrite, not new design territory (finding class degraded: core bugs → contract gaps → editorial consistency = convergence). Fixes: (1) announcement no longer reads provenance AT ALL — current version = baked `CANON_VERSION` (the updater IS the package being updated), current SHA = tolerant parse of the install-root manifest's own `canon-ai` dependency pin (`github:…#<40hex>`), a file AC-3's guard already reads; display-only, `unknown` on any odd shape; AC-5 gains a no-read assertion (announcement identical with/without a provenance file present) — this also plants the follow-up's package.json-identity idea. Provenance file is now strictly write-only (Decision 4/7 + Data Model swept). (2) `deps` seam gains an `exit` override mirroring the existing `stopCmd`/`watchCmd` convention (verified real: `stop.ts:370,405` `deps.exit ?? process.exit`); refusal ACs (3/4/7/8) observe non-zero exit via the injected override. (3) AC-10 docs reworded: provenance described strictly as written-for-future-tooling, README `canon doctor` row explicitly NOT edited, no current-consumption claim allowed. Human Test Plan step 1 softened for the first-run unknown-current-SHA case.

[spec_review] Fresh Round-8 review: no blocking findings. Passing nits forward: the AC-2 fixture sketch does not say whether the local `.canon/` parent exists even though the successful updater writes `.canon/provenance.json`; the plan should either seed it or require recursive creation. The resolver contract requires `GIT_TERMINAL_PROMPT=0`, but no acceptance test observes that environment value in the fake-git path. The plan should also make the resolver result shape explicit because stable announcement/provenance need both the selected final tag and its peeled commit SHA.

[spec_review] Fresh Round-7 review: AC-2 declares `install/` and `adopter/` as siblings but then invokes `node install/node_modules/canon-ai/dist/cli/index.js update` with cwd set to `adopter/`; that relative path resolves under `adopter/install/`, so the subprocess cannot launch the fixture as written. The stable resolver also says to choose the highest SemVer `v*` tag without excluding prereleases, so a tag such as `v9.0.0-rc.1` can outrank an older final release and be installed on the stable channel.

[spec] Round-7 revision (Claude, conversational, operator-approved) — two fixes + ALTITUDE TRIM. Fixes: (1) stable selection now considers only strict `vX.Y.Z` tags — prerelease/suffixed tags excluded from stable (AC-6 case iii: `v9.0.0-rc.1` above `v8.2.0` → final wins); a tag universe with no final release ABORTS (AC-7 third case). Strict-form filter kills the whole odd-tag class, not just `-rc`. (2) AC-2's fixture entrypoint invoked by absolute path — the round-7 relative-path bug existed only in spec pseudocode; behavioral phrasing removes the class. Trim rationale (diagnosed with operator after 7 rounds): the spec had drifted to code altitude — regexes, transcripts, seam member lists, literal invocations — so spec_review was reviewing untyped pseudocode at 30min/round, and rounds 6–7 findings were bugs in text the PREVIOUS revision added (a ratchet: each "underspecified" finding answered with more prose = more reviewable surface). Restructure: ACs state behavioral contracts only; mechanics moved to a new "Implementation Notes (non-binding)" subsection explicitly owned by plan/implement with compiler+tests as checker; header carries an altitude note so the reviewer knows the boundary is deliberate. Candidate lessons-learned entry at QA: spec_review has no altitude governor — an over-mechanized spec silently converts spec review into code review of pseudocode; also a possible canon-spec/spec-template guardrail ("mechanics belong in Implementation Notes / plan").

[spec] Round-4 revision (Claude) — resolved all 3 blocking spec_review issues, all confined to AC-10 (doctor provenance). Round-shape check at ≥3 iters: rounds 1–2 hit update.ts core (packageDir seam, fork-override); round-3 revision cleared 4 update.ts/resolver issues; this round is doctor's FIRST focused review, not a sub-problem area expanding round-over-round → edge-fine-tune, revise in place (AC-10's pre-declared trim valve reserved for a later round that keeps surfacing NEW doctor sub-problems). Fixes: (1) recast AC-10 + Decision item 8 as an ORDERED decision (first-match-wins) with stale/mismatch (branch 4) explicitly evaluated before stable-pass (branch 5) — closes the overlap; (2) added malformed branch (branch 2): unparseable JSON or schema-invalid (channel enum, 40-hex resolved_sha, stable-version presence) → warn-not-throw, mirroring readAllowFromSettings malformed-JSON handling (doctor.ts:446-458), with per-violation tests; (3) lookup-root contract — checkCanonProvenance reads `process.cwd()` (the current repo), matching every other canon check in doctorCmd(); does NOT resolve the running package's install root — cross-cwd AC-2 shape lands in the absent branch (branch 1) rather than reading a foreign file. Also renumbered stale AC-10(d) refs → "AC-10 branch 4"; added 2 Known Risks (doctor whole-repo contract, hand-editable provenance).


[2026-07-18] Operator accepted code_review via `canon task accept` — sanctioned (agent verdict overridden). Reason: pnpm-global's prior 'working' npm install -g was itself a #188-class wrong-target mutation (npm's global prefix, not the running pnpm store) that exited 0 without updating the running install; fail-closed refusal is strictly more honest. Spec Known Risks already declares pnpm out of scope without a local/global carve-out. Verified safe: no install or provenance write can land at a wrong directory. pnpm-aware refusal-message guidance deferred to backlog as XS follow-up..

[implement-reroute] Importing the updater also imports the shared environment module, which performs a local git probe during initialization; the red-first fixture therefore records an extra non-resolution git call. Filtering the recorder to `ls-remote` calls keeps the AC-12 order assertion focused while the environment assertion still covers every invocation.

[implement-reroute] A resolver retry must preserve the complete logical `ls-remote` argument list and replace only the remote URL. The shared fallback helper centralizes that invariant for stable tags and named refs, preventing the two paths from drifting.

[implement-revision] `GIT_SSH_COMMAND` is a user-level transport configuration, not an internal scratch value. The resolver now preserves custom command arguments/identity settings and adds `BatchMode=yes`, including replacing common explicit `BatchMode=no` options so the non-interactive guarantee remains enforced.

