# Notes

Raw observations from any phase. Prefix with phase name. Distilled into `docs/lessons-learned.md` during QA.

<!-- Append below this line -->

[spec_review] The existing spec-review loop-cap message instructs direct edits to only `status` and `iterations_current_loop`, while `canon task reset-spec-review <id>` also archives the cumulative review, clears the verdict, and drops related router state. Requested that the centralized message use the canonical helper so stale review evidence cannot survive the advertised recovery path.

[spec] Round-1 revision: accepted the blocking finding as stated. Confirmed the three things a hand-edit misses (`spec-review.md` archive, `verdict` clear, `sessions.claude_spec` drop) against `taskResetSpecReview()` at `src/task/index.ts:1040-1071`. One precision note on the reviewer's reasoning: `taskPhase()` *does* clear a stale verdict on a `pending` transition (`src/task/index.ts:456-459`) — the hazard is that the advertised recovery is a raw file edit that bypasses `taskPhase()` entirely, so it gets none of that. Same conclusion, sharper mechanism; recorded in *Problem*. Generalized former AC-4 into AC-4 (recovery ordering + canonical reset command, both phases) + AC-5 (no hand-edit instruction in either emitted reason, asserted by regex on the builder output); renumbered the tail to AC-8. Also extended the `docs/pipeline-orchestrator.md` row so the doc's recovery line (line 372, `reset-code-review` only) names the `spec_review` parallel — otherwise the doc and the corrected message would disagree.

[spec] Iteration-shape check at round 1: edge-fine-tune (one missed message surface inside a change already centralizing that text), not scope-expansion. No AC redesign warranted.

[spec_review] Fresh round-2 trace found a recovery-order gap in the proposed pre-route block: `autoBlockPhase()` leaves the review phase current, so raising `MAX_REVIEW_LOOPS` and rerunning dispatches review against the unchanged rejected spec/implementation instead of first running the skipped `routeBackTo()` revision target. Requested a persisted continuation contract plus cap-raised resume tests for both review loops.

[spec] Round-2 revision: accepted the blocker as a design finding and redesigned the mechanism rather than bolting a continuation field onto the round-1 shape. New shape: leave `checkAndRoute()` untouched (its `routeBackTo` IS the persisted continuation) and move each loop's cap checkpoint from the *review* phase entry to the *revision* phase entry (`runSpecPhase`, `runImplementPhase`), above every side effect. Post-block state is revision=pending + review=blocked, so `deriveTopLevelStatus()` makes the revision current: cap-raised resume runs revision→review in order, bare resume re-blocks at the same guard with zero agent spawn. No new `status.json` field. Deleted the round-1 `checkAndRoute` checks and the `StatusJson[]`-native cap accessor AC (both revision entries already have `TaskContext[]` in scope). Verified the enabling fact independently: nothing anywhere refuses to dispatch a `blocked` phase — `'blocked'` appears only in `autoBlockPhase`'s write, the reroute detector (`main.ts:2351`), and `VALID_STATUSES`.

[spec] Round-2 pre-flight ground truth that shaped the ACs: (1) `TaskContext.iterations_current_loop` is populated from `code_review` ONLY (`buildPipelineState`, main.ts:218-227) — the spec loop must read `status.phases.spec_review` with the legacy `iterations` fallback, or it silently never blocks; (2) `rerouteFromHumanReview()` zeroes both `iterations_current_loop` and `preflight_rejections_current_loop` (main.ts:2461-2500), which is why revision-entry guards are inert on reroute — no exclusion branch needed; (3) pre-flight rejections route back to `implement` through the same `anyChangesRequested` branch, so the `implement`-entry guard now catches pre-flight-driven runaway loops a cycle earlier; (4) `--reroute` from a loop-cap block is refused today (needs a `spec_gap` verdict) and stays refused — the current-phase change from `code_review` to `implement` doesn't regress it.

[spec] Iteration-shape check at round 2: **shape/design**, not scope-expansion — one finding, that the proposed mechanism had no working continuation path. Response per the ≥3-iteration rule is AC redesign, done here. No new sub-problem was introduced.

[spec] ADJACENT LIVE DEFECT (recorded, not fixed — see spec AC-14 / Non-Goals). `promptSpecRevision` is unreachable through the normal loop. `runSpecPhase` picks the revision prompt via `t.specReviewVerdict === 'changes_requested'` (phases/spec.ts:17), but `routeBackTo('spec')` clears `spec_review.verdict` before the phase is entered and `buildPipelineState()` re-reads it from disk each iteration — so every full-tier spec revision gets the first-write `promptSpec`. Evidence from THIS task's own run: `.canon-run.log` lines 36 and 155 both print `Phase: spec (Claude writes spec)` for revision rounds 1 and 2. The implement side dodges this by keying off counters, with a comment saying why (`shouldUseImplementRevision`, phases/implement.ts:11-24). Fix direction: mirror the counter-based signal. Kept out of this task — separate defect class, changes the prompt every full-tier revision receives, needs its own red-first test.

[spec_review] Fresh round-3 trace found that the redesigned code-loop block persists `implement` as current, but `taskResetCodeReview()` rejects every current phase except `code_review`. The reason-builder AC and Human Test Plan advertise an unusable fallback while Non-Goals forbid changing the helper; requested either safe helper support for the new state or a different recovery, plus a command-level regression test.

[spec] Round-3 revision: accepted the blocker. Verified it directly — `taskResetCodeReview()` throws unless `deriveTopLevelStatus(status) === 'code_review'` (`src/task/index.ts:1084-1088`), and the round-2 post-block state derives `implement`, so the fallback the message advertises is refused. Took the reviewer's option 1 (widen the helper) over option 2 (name a different recovery), because the widening is provably a no-op on every state accepted today: `deriveTopLevelStatus()` returns the first non-`done` phase in `PHASE_ORDER` and `implement` precedes `code_review` (`state.ts:367-373`, `types.ts:12`), so any task deriving `code_review` already has `implement.status === 'done'`. New AC-16 (one-state widening + the mirror `implement` → `done` write, with both existing task-CLI tests passing unmodified and a new over-widening rejection case) and AC-17 (the command-level regression test the reviewer asked for, run against the persisted AC-1/AC-2 fixtures). Amended the Non-Goal that had ruled the helper out, and added `src/task/index.ts` + `tests/task-cli.test.ts` to Affected Files.

[spec] Round-3 finding the reviewer did NOT flag, now pinned: the two reset helpers are asymmetric. `taskResetSpecReview()` has no phase precondition at all (`src/task/index.ts:1040-1072`), so it already accepts the post-block state — the spec side has no defect. That asymmetry becomes load-bearing after this change, so AC-17's spec half is a contract-lock: a later "harmonize the two helpers" cleanup that adds a `reset-code-review`-style guard must fail a test rather than silently recreate the trap on the spec side. Non-Goals now names that cleanup as out of scope.

[spec] Round-3 scope check on the newly-accepted state: `implement`-current + `code_review`-blocked is reachable ONLY through this task's new checkpoint. The other two writers of a blocked `code_review` — `spec_gap` (`main.ts:3150-3185`) and pre-flight-infrastructure (`code-review.ts:298-308`) — both fire with `implement` already `done`, so they still derive `code_review` and are unaffected by the widening. Also confirmed no orchestrator code calls the helper (`grep -rn "taskResetCodeReview" scripts/` empty); only the CLI dispatch at `src/task/index.ts:1567`.

[spec] Iteration-shape check at round 3: **edge-fine-tune on the round-2 shape**, not scope-expansion. The finding is a direct consequence of the round-2 relocation (which phase is current post-block), not a new sub-problem. Round-over-round: 1 = edge-fine-tune, 2 = design/shape (redesigned), 3 = edge-fine-tune completing that redesign. Converging; no further AC redesign warranted.

[spec_review] Fresh round-4 trace found that the two call sites sharing each loop-cap evaluator do not share the same resume order. A revision-entry block leaves the revision pending, but the retained review-entry backstop has that revision already done; after a cap raise it resumes review directly. The unconditional AC-10 message promising revision first is therefore false at the AC-11 checkpoint. Requested checkpoint-aware/neutral guidance and tests for both persisted states.

[spec] Round-4 revision: accepted the blocker; verified it independently before writing (`autoBlockPhase` touches only the review phase, `state.ts:399-416`; `deriveTopLevelStatus` returns the first non-`done` phase, `state.ts:367-373`; so revision-`pending` derives revision, revision-`done` derives the `blocked` review). One sharpening the reviewer didn't state: the false sentence is not merely *printed* — `autoBlockPhase` persists the reason into `escalations[].reason`, so wrong recovery guidance is durable on disk. Took "checkpoint-aware" over "neutral": neutral wording would discard the operator-facing value round 2 added. Mechanism choice matters — the clause is derived *inside* the single evaluator from the revision phase's persisted status (the same fact the router reads) rather than from a caller-passed checkpoint flag, because a passed flag can contradict the router and a derived fact cannot. AC-10 rewritten to assert the persisted reason in both states, AC-11 gained backstop message assertions + an explicit fixture shape, AC-6 gained (d): a pair-test proving the two reasons differ ONLY in that clause (kills both drift shapes — hard-coded clause, or per-call-site duplicated reason).

[spec] Round-4 trap worth recording: `spec` is a substring of `spec_review`, so an unanchored `/spec/` match satisfies AC-10 in BOTH states and the discrimination silently evaporates with a green suite. AC-10 mandates word-boundary anchoring (`/\bspec\b/` does not match `spec_review` — `_` is a word char, so there's no boundary after `spec`); also recorded in Known Risks.

[spec] Round-4 nit (AC-17 cross-file temp-state dependency): took neither of the reviewer's two options literally. Option A (invoke the reset inside the same integration fixture) is strictly better than the shared-fixture-helper option — it removes the drift risk instead of locking it — and it's viable because `taskCmd` is exported from `src/task/index.ts` and importable by `tests/run-task-safety.test.ts`, so the reset runs in-process against the fixture the same test just blocked, inside its own lifetime. AC-17 moved out of `tests/task-cli.test.ts` (which keeps AC-16 only); Affected Files rows updated both ways.

[spec] Round-4 class closure: rounds 3 and 4 are the SAME root cause — the round-2 relocation changed which phase is current post-block, and two separate things assumed otherwise (r3: a reset precondition; r4: the resume-order guidance). Rather than wait for instance three, enumerated every non-normalizing consumer of the derived current phase and classified each: `taskResetCodeReview` precondition (`src/task/index.ts:1085`, fixed by AC-16); `assertSamePhase` (`main.ts:238`, satisfied by construction — which also proves mixed bundles are unreachable at either checkpoint); `--ship`'s gate (`main.ts:2070`/`2082`, blocked task is at neither phase before or after); `--reroute` eligibility (`main.ts:2348-2356` — today it passes the phase conjunct and fails `someSpecGap`, after the change it fails both: refused either way); three display surfaces that print the true current phase (`derivePhase` for `canon task list` `src/task/index.ts:257-259`, `canon watch` `src/cli/commands/watch.ts:128`, the `--reroute` refusal summary `main.ts:2358`) — now show `implement`/`spec`, which is accurate, not stale. Remaining `deriveTopLevelStatus()` call sites are `status.status` normalization writes, not gates. Recorded in *Interaction Dependencies* so it's a decision, not an oversight.

[spec] Iteration-shape check at round 4: **edge-fine-tune** — no new files, no new sub-problem, one state-dependent clause plus assertions on fixtures the spec already defines. Round-over-round: 1 edge → 2 design/shape (redesigned) → 3 edge (relocation consequence) → 4 edge (relocation consequence, class now closed). The relocation itself is unchanged and still the right shape, so no AC redesign. Honest caveat: two consecutive findings from one under-analyzed round-2 consequence is a real signal — the response was to close the class, not to patch instance two.

[spec] Round-4 operational note: `spec_review.iterations_current_loop` is 4 with `auto_block_count` 1, so the cap is already raised past the M default of 3. Round 5 needs `MAX_REVIEW_LOOPS=5` (or higher) or it auto-blocks at the entry guard instead of reviewing — same never-reset-the-counter rule as before.

[implement] Red-first subprocess fixtures reproduced the specified waste: the capped spec entry logged one `claude` invocation and both capped code entries (review iterations alone and review + pre-flight combined) logged one `codex` invocation before the fix. The retained spec-review backstop also persisted the old field-assignment recovery text and no state-derived resume phase.

[implement] Scope blocker discovered only after the required fresh build: AC-16 changes `src/task/index.ts`, which is bundled into both published entry points, so `npm run build` necessarily rewrites `dist/cli/index.js` as well as the spec-declared `dist/scripts/run-task.js`. The Affected Files table omitted `dist/cli/index.js`; completion cannot honestly satisfy both the scope cap and the committed-dist freshness gate without a spec amendment/authorization.

[implement] The resumed implementation direction explicitly required completion after the scope omission was surfaced. Kept the required `dist/cli/index.js` output, reran the full validation matrix, and recorded the generated-artifact deviation in the handoff.

[implement-revision] A state-derived resume clause is insufficient when an earlier sentence in the same persisted reason makes the opposite state claim. Pair tests should pin invariant facts (count, cap, command order) while allowing every state-dependent sentence to vary together.

[implement-revision] Tightening reset-code-review requires checking the concrete revision status, not only the derived current phase: both pending and in-progress implementations derive to `implement`, but only pending is the persisted pre-route block state.

[implement-revision] The review surfaced two binding gaps that cannot be silently resolved here: task accept needs an explicit semantic choice for the new block state, and the stale recovery guidance spans files outside the spec's Affected Files table (including a canon-managed mirror).

[implement-revision] The full suite's PR-flow fixtures run docs reference validation over live task artifacts. A malformed multi-location citation in reviewer-owned review.md can therefore preempt otherwise unrelated PR tests; the artifact is outside this task's implementation scope and was not edited.


[implement-reroute] A plain raised-cap integration fixture must update the review artifact as well as status.json: clean-exit evidence recovery can legitimately replace a fake agent's status-only verdict with the cumulative artifact verdict. The logging fake also needs to run its completer for a phase sequence, not only a single fixed phase.

[implement-revision] Recovery guidance must disclose every state-normalizing write, not just archived artifacts and counters. Here, `reset-code-review` distinguishes two materially different operator choices: reset accepts the current implementation as-is for re-review, while raising the cap requests another implementation pass.

[implement-revision] Reset disclosure is a cross-loop invariant, but cap-raise guidance is state-dependent: it can promise the deferred revision only while `spec` / `implement` is pending. A shared helper must own both facts so primary and backstop reasons cannot contradict their resume clause.


